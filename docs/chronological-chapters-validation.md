# 时间顺序章节验收

2026-09-21，使用真实用户身份创建飞书测试文档，复用此前合成语音的真实 ASR 文本；本次只验证文档编排，示例时段不代表真实主播录音时间。

写入顺序故意为：李四12:00–18:00、王五18:00–次日00:00、张三08:00–12:00的10点片段、再补张三9点片段。

实际读回：
1. 08:00–12:00 · 验收张三，9点文字在10点之前。
2. 12:00–18:00 · 验收李四。
3. 18:00–次日00:00 · 验收王五。

重新初始化同步器后重复运行，文档内容不变。场次表三条测试记录指向同一份文档。

实际验收文档：https://guanghe.feishu.cn/docx/U1jAd26BtoZ0QRx6BiLcn24knp3

发现并修复：飞书只传 end_block_id 的 range 返回单块，不能查到标题之前的插入位置。补入更早章节时读取带 ID 的文档结构，定位前一个顶层块后插入。已加入回归测试。

录制仍为每小时，不重启 MediaMTX；本次仅更新并重启飞书同步服务。部署前确认生产没有采用旧按主播布局的每日文档回执，因此新每日文档直接使用时间章节。
